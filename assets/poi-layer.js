window.createPOILayer=function(map,places,geo,onSelect){
  const icons={cafe:'🍴',falls:'💧',beach:'🏖',reef:'🐠',church:'⛪',tree:'🌳',cave:'◒',mountain:'⛰'};
  const list=Object.entries(places).filter(([id,p])=>geo.places[id]?.lngLat&&(icons[p.kind]||p.type==='해변')).map(([id,p])=>({id,p,point:geo.places[id].lngLat,icon:icons[p.kind]||'🏖'}));
  const holder=document.createElement('div');holder.className='poi-layer';document.getElementById('world').appendChild(holder);let route=new Set(),selected='';
  list.sort((a,b)=>Number(!!b.p.extra)-Number(!!a.p.extra));
  for(const a of list){const el=document.createElement('button');el.className='poi-icon '+(a.p.kind==='cafe'?'food':'sight');el.setAttribute('aria-label',a.p.name+' · '+a.p.type);el.title=a.p.name;el.innerHTML='<b>'+a.icon+'</b><span>'+a.p.name+'</span>';el.onclick=()=>onSelect(a.id);holder.appendChild(el);a.el=el;}
  function draw(){const zoom=map.getZoom(),w=map.getCanvas().clientWidth,h=map.getCanvas().clientHeight,placed=[],gap=w<=900?32:36,panels=[...document.querySelectorAll('.scene-header,.scene-player,#scene-place,.place-panel,.day-nav,#scene-menu')].map(e=>e.getBoundingClientRect()).filter(r=>r.width&&r.height);let visible=0;for(const a of [...list].sort((a,b)=>Number(b.id===selected)-Number(a.id===selected))){const p=map.project(a.point);const show=zoom>=9&&!route.has(a.id)&&p.x>15&&p.y>85&&p.x<w-15&&p.y<h-85&&!placed.some(q=>Math.abs(p.x-q.x)<gap&&Math.abs(p.y-q.y)<gap)&&!panels.some(r=>p.x>r.left-16&&p.x<r.right+16&&p.y>r.top-16&&p.y<r.bottom+16);a.el.hidden=!show;if(show){placed.push(p);visible++;a.el.style.transform=`translate(${p.x}px,${p.y}px) translate(-50%,-50%)`;a.el.classList.toggle('selected',selected===a.id);}}window.poiState={total:list.length,visible,extra:list.filter(a=>a.p.extra).length};}
  map.on('move',draw);draw();return{list,refresh:draw,update(ids,current){route=new Set(ids);selected=current;draw();},select(id){selected=id;draw();},remove(){map.off('move',draw);holder.remove();}};
};
